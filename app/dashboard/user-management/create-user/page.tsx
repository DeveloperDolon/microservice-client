import { useForm } from "react-hook-form";

const page = () => {
    const methods = useForm({
        resolver: 
    });
  return (
    <div>
      Hello world from create user.
    </div>
  );
};

export default page;